server:
  port: 48091
spring:
  application:
    name: adapter-server
  profiles:
    active: local
  threads:
    virtual:
      enabled: true
  config:
    import:
      - optional:classpath:application-${spring.profiles.active}.yaml # 加载【本地】配置

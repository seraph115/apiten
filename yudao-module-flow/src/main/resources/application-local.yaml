--- #################### 注册中心相关配置（P0 暂未启用，见 application.yaml） ####################

spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:18848 # Nacos 服务器地址（本地 docker-compose 中间件，见 docker/docker-compose.yml）
        namespace: dev # 命名空间，与 yudao-gateway 保持一致，便于服务发现互通
